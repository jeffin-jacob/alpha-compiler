type T1: integer -> integer
function square: T1
square(x) := {
    [integer: a; integer: b]
    a := 2;
    b := 5;
    if (a=b) then {
        x:= x+x;
        x:= 10;
    }
    else {
        x:= 5;
    }
}
